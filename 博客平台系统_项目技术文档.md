# 博客平台系统 - 项目技术文档

## 📋 项目概述

基于 Spring Boot 3.x + Vue3 的前后端分离博客平台系统，支持用户登录、博客发表、博客搜索等功能。

---

## 🛠️ 技术栈详情

### 后端技术栈

| 技术            | 版本   | 说明                             |
| --------------- | ------ | -------------------------------- |
| Java            | 17+    | JDK 版本（Spring Boot 3.x 要求） |
| Spring Boot     | 3.2.x  | 核心框架                         |
| Spring Security | 6.x    | 安全认证框架                     |
| MyBatis Plus    | 3.5.x  | ORM 框架                         |
| MySQL           | 8.0+   | 关系型数据库                     |
| Redis           | 7.x    | 缓存、Session 存储               |
| JWT             | 0.12.x | Token 认证                       |
| Lombok          | 1.18.x | 简化代码                         |
| Knife4j         | 4.x    | API 文档                         |

### 前端技术栈

| 技术         | 版本  | 说明         |
| ------------ | ----- | ------------ |
| Vue          | 3.4.x | 前端框架     |
| Vite         | 5.x   | 构建工具     |
| Element Plus | 2.5.x | UI 组件库    |
| Axios        | 1.6.x | HTTP 客户端  |
| Vue Router   | 4.x   | 路由管理     |
| Pinia        | 2.x   | 状态管理     |
| Sass         | 1.x   | CSS 预处理器 |

### 开发工具

| 工具          | 说明            |
| ------------- | --------------- |
| pnpm          | Monorepo 包管理 |
| Git           | 版本控制        |
| VSCode / IDEA | IDE             |

---

## 🖥️ 环境配置（Windows）

### 1. 安装 JDK 17

```powershell
# 下载 JDK 17 (推荐使用 Adoptium)
# 访问: https://adoptium.net/

# 或使用 winget 安装
winget install EclipseAdoptium.Temurin.17.JDK

# 验证安装
java -version
```

**配置环境变量：**
```powershell
# 设置 JAVA_HOME (以管理员身份运行 PowerShell)
[Environment]::SetEnvironmentVariable("JAVA_HOME", "C:\Program Files\Eclipse Adoptium\jdk-17.0.x", "Machine")
[Environment]::SetEnvironmentVariable("Path", $env:Path + ";%JAVA_HOME%\bin", "Machine")
```

### 2. 安装 Node.js

```powershell
# 使用 winget 安装 Node.js LTS
winget install OpenJS.NodeJS.LTS

# 验证安装
node -v
npm -v

# 安装 pnpm
npm install -g pnpm

# 验证 pnpm
pnpm -v
```

### 3. 安装 MySQL 8.0

```powershell
# 使用 winget 安装
winget install Oracle.MySQL

# 或下载安装包: https://dev.mysql.com/downloads/mysql/

# 启动 MySQL 服务
net start mysql

# 登录 MySQL
mysql -u root -p
```

**创建数据库：**
```sql
-- 创建数据库
CREATE DATABASE blog_platform CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- 创建用户（可选）
CREATE USER 'blog_user'@'localhost' IDENTIFIED BY 'your_password';
GRANT ALL PRIVILEGES ON blog_platform.* TO 'blog_user'@'localhost';
FLUSH PRIVILEGES;
```

### 4. 安装 Redis

```powershell
# 方式1: 使用 winget
winget install Redis.Redis

# 方式2: 使用 Chocolatey
choco install redis-64

# 方式3: 下载 Windows 版本
# https://github.com/tporadowski/redis/releases

# 启动 Redis
redis-server

# 测试连接
redis-cli ping
# 应返回: PONG
```

### 5. 安装 Git

```powershell
winget install Git.Git

# 配置 Git
git config --global user.name "Your Name"
git config --global user.email "your.email@example.com"
```

### 6. 安装 IDE

```powershell
# IntelliJ IDEA (后端开发)
winget install JetBrains.IntelliJIDEA.Community

# VSCode (前端开发)
winget install Microsoft.VisualStudioCode
```

---

## 📁 项目结构 (Monorepo)

```
blog-platform/
├── pnpm-workspace.yaml          # pnpm 工作空间配置
├── package.json                  # 根 package.json
├── .gitignore                   # Git 忽略文件
├── README.md                    # 项目说明
│
├── backend/                     # 后端项目 (Spring Boot)
│   ├── pom.xml                  # Maven 配置
│   ├── src/
│   │   ├── main/
│   │   │   ├── java/com/blog/
│   │   │   │   ├── BlogApplication.java
│   │   │   │   ├── config/           # 配置类
│   │   │   │   │   ├── SecurityConfig.java
│   │   │   │   │   ├── RedisConfig.java
│   │   │   │   │   ├── CorsConfig.java
│   │   │   │   │   └── MyBatisConfig.java
│   │   │   │   ├── controller/       # 控制器
│   │   │   │   │   ├── AuthController.java
│   │   │   │   │   ├── UserController.java
│   │   │   │   │   ├── BlogController.java
│   │   │   │   │   └── CommentController.java
│   │   │   │   ├── service/          # 服务层
│   │   │   │   │   ├── AuthService.java
│   │   │   │   │   ├── UserService.java
│   │   │   │   │   ├── BlogService.java
│   │   │   │   │   └── impl/
│   │   │   │   ├── mapper/           # MyBatis Mapper
│   │   │   │   │   ├── UserMapper.java
│   │   │   │   │   ├── BlogMapper.java
│   │   │   │   │   └── CommentMapper.java
│   │   │   │   ├── entity/           # 实体类
│   │   │   │   │   ├── User.java
│   │   │   │   │   ├── Blog.java
│   │   │   │   │   └── Comment.java
│   │   │   │   ├── dto/              # 数据传输对象
│   │   │   │   ├── vo/               # 视图对象
│   │   │   │   ├── common/           # 通用类
│   │   │   │   │   ├── Result.java
│   │   │   │   │   └── PageResult.java
│   │   │   │   ├── exception/        # 异常处理
│   │   │   │   ├── security/         # 安全相关
│   │   │   │   │   ├── JwtUtils.java
│   │   │   │   │   └── JwtFilter.java
│   │   │   │   └── utils/            # 工具类
│   │   │   └── resources/
│   │   │       ├── application.yml
│   │   │       ├── application-dev.yml
│   │   │       └── mapper/           # XML 映射文件
│   │   └── test/
│   └── target/
│
├── frontend/                    # 前端项目 (Vue3)
│   ├── package.json
│   ├── vite.config.js
│   ├── index.html
│   ├── src/
│   │   ├── main.js
│   │   ├── App.vue
│   │   ├── api/                 # API 请求
│   │   │   ├── index.js
│   │   │   ├── auth.js
│   │   │   ├── user.js
│   │   │   └── blog.js
│   │   ├── views/               # 页面组件 (8个以上)
│   │   │   ├── Home.vue              # 1. 首页
│   │   │   ├── Login.vue             # 2. 登录页
│   │   │   ├── Register.vue          # 3. 注册页
│   │   │   ├── BlogList.vue          # 4. 博客列表页
│   │   │   ├── BlogDetail.vue        # 5. 博客详情页
│   │   │   ├── BlogEdit.vue          # 6. 博客编辑页
│   │   │   ├── UserProfile.vue       # 7. 用户主页
│   │   │   ├── UserSettings.vue      # 8. 用户设置页
│   │   │   ├── Search.vue            # 9. 搜索结果页
│   │   │   └── About.vue             # 10. 关于页面
│   │   ├── components/          # 公共组件
│   │   │   ├── NavBar.vue
│   │   │   ├── Footer.vue
│   │   │   ├── BlogCard.vue
│   │   │   └── CommentList.vue
│   │   ├── router/              # 路由配置
│   │   │   └── index.js
│   │   ├── stores/              # Pinia 状态管理
│   │   │   ├── user.js
│   │   │   └── blog.js
│   │   ├── utils/               # 工具函数
│   │   │   ├── request.js
│   │   │   └── auth.js
│   │   └── styles/              # 样式文件
│   │       └── global.scss
│   └── public/
│
├── docs/                        # 项目文档
│   ├── 需求分析.md
│   ├── 可行性分析.md
│   ├── 概要设计.md
│   ├── 详细设计.md
│   ├── 项目分工.md
│   └── 开发过程文档.md
│
└── sql/                         # 数据库脚本
    ├── init.sql                 # 初始化脚本
    └── data.sql                 # 测试数据
```

---

## 🚀 项目初始化命令

### 1. 创建项目目录结构

```powershell
# 创建主目录
mkdir blog-platform
cd blog-platform

# 创建子目录
mkdir backend, frontend, docs, sql

# 初始化 Git
git init
```

### 2. 初始化 Monorepo

创建 `pnpm-workspace.yaml`:
```yaml
packages:
  - 'frontend'
```

创建根目录 `package.json`:
```json
{
  "name": "blog-platform",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:frontend": "pnpm --filter frontend dev",
    "build:frontend": "pnpm --filter frontend build",
    "dev:backend": "cd backend && mvn spring-boot:run"
  }
}
```

### 3. 初始化后端项目

```powershell
cd backend

# 使用 Spring Initializr 或手动创建 pom.xml
```

**后端 `pom.xml`:**
```xml
<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.2.1</version>
    </parent>

    <groupId>com.blog</groupId>
    <artifactId>blog-backend</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>

    <properties>
        <java.version>17</java.version>
        <mybatis-plus.version>3.5.5</mybatis-plus.version>
    </properties>

    <dependencies>
        <!-- Spring Boot Starters -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-redis</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- MyBatis Plus -->
        <dependency>
            <groupId>com.baomidou</groupId>
            <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
            <version>${mybatis-plus.version}</version>
        </dependency>

        <!-- MySQL -->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- JWT -->
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-api</artifactId>
            <version>0.12.3</version>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-impl</artifactId>
            <version>0.12.3</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-jackson</artifactId>
            <version>0.12.3</version>
            <scope>runtime</scope>
        </dependency>

        <!-- Knife4j API文档 -->
        <dependency>
            <groupId>com.github.xiaoymin</groupId>
            <artifactId>knife4j-openapi3-jakarta-spring-boot-starter</artifactId>
            <version>4.4.0</version>
        </dependency>

        <!-- Lombok -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
```

**后端 `application.yml`:**
```yaml
server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/blog_platform?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: your_password

  data:
    redis:
      host: localhost
      port: 6379
      database: 0

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

# JWT配置
jwt:
  secret: your-256-bit-secret-key-for-jwt-authentication
  expiration: 86400000  # 24小时

# Knife4j配置
knife4j:
  enable: true
  setting:
    language: zh_cn
```

### 4. 初始化前端项目

```powershell
cd frontend

# 使用 Vite 创建 Vue3 项目
pnpm create vite . --template vue

# 安装依赖
pnpm install

# 安装额外依赖
pnpm add element-plus @element-plus/icons-vue
pnpm add axios vue-router@4 pinia
pnpm add sass -D
```

**前端 `vite.config.js`:**
```javascript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import { resolve } from 'path'

export default defineConfig({
  plugins: [vue()],
  resolve: {
    alias: {
      '@': resolve(__dirname, 'src')
    }
  },
  server: {
    port: 5173,
    proxy: {
      '/api': {
        target: 'http://localhost:8080',
        changeOrigin: true
      }
    }
  }
})
```

---

## 📊 数据库设计

### 数据库初始化脚本 (`sql/init.sql`)

```sql
-- 创建数据库
CREATE DATABASE IF NOT EXISTS blog_platform 
CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

USE blog_platform;

-- 用户表
CREATE TABLE `user` (
    `id` BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '用户ID',
    `username` VARCHAR(50) NOT NULL UNIQUE COMMENT '用户名',
    `password` VARCHAR(255) NOT NULL COMMENT '密码(加密)',
    `email` VARCHAR(100) UNIQUE COMMENT '邮箱',
    `nickname` VARCHAR(50) COMMENT '昵称',
    `avatar` VARCHAR(255) COMMENT '头像URL',
    `bio` TEXT COMMENT '个人简介',
    `role` TINYINT DEFAULT 0 COMMENT '角色: 0-普通用户, 1-管理员',
    `status` TINYINT DEFAULT 1 COMMENT '状态: 0-禁用, 1-正常',
    `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    INDEX `idx_username` (`username`),
    INDEX `idx_email` (`email`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';

-- 博客分类表
CREATE TABLE `category` (
    `id` BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '分类ID',
    `name` VARCHAR(50) NOT NULL UNIQUE COMMENT '分类名称',
    `description` VARCHAR(255) COMMENT '分类描述',
    `sort_order` INT DEFAULT 0 COMMENT '排序',
    `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间'
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='博客分类表';

-- 博客表
CREATE TABLE `blog` (
    `id` BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '博客ID',
    `user_id` BIGINT NOT NULL COMMENT '作者ID',
    `category_id` BIGINT COMMENT '分类ID',
    `title` VARCHAR(200) NOT NULL COMMENT '标题',
    `summary` VARCHAR(500) COMMENT '摘要',
    `content` LONGTEXT NOT NULL COMMENT '内容(Markdown)',
    `cover_image` VARCHAR(255) COMMENT '封面图片',
    `view_count` INT DEFAULT 0 COMMENT '浏览量',
    `like_count` INT DEFAULT 0 COMMENT '点赞数',
    `comment_count` INT DEFAULT 0 COMMENT '评论数',
    `status` TINYINT DEFAULT 1 COMMENT '状态: 0-草稿, 1-已发布, 2-已删除',
    `is_top` TINYINT DEFAULT 0 COMMENT '是否置顶',
    `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    FOREIGN KEY (`user_id`) REFERENCES `user`(`id`),
    FOREIGN KEY (`category_id`) REFERENCES `category`(`id`),
    INDEX `idx_user_id` (`user_id`),
    INDEX `idx_category_id` (`category_id`),
    INDEX `idx_created_at` (`created_at`),
    FULLTEXT INDEX `idx_fulltext` (`title`, `content`) WITH PARSER ngram
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='博客表';

-- 标签表
CREATE TABLE `tag` (
    `id` BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '标签ID',
    `name` VARCHAR(50) NOT NULL UNIQUE COMMENT '标签名称',
    `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间'
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='标签表';

-- 博客标签关联表
CREATE TABLE `blog_tag` (
    `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
    `blog_id` BIGINT NOT NULL,
    `tag_id` BIGINT NOT NULL,
    FOREIGN KEY (`blog_id`) REFERENCES `blog`(`id`) ON DELETE CASCADE,
    FOREIGN KEY (`tag_id`) REFERENCES `tag`(`id`) ON DELETE CASCADE,
    UNIQUE INDEX `idx_blog_tag` (`blog_id`, `tag_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='博客标签关联表';

-- 评论表
CREATE TABLE `comment` (
    `id` BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '评论ID',
    `blog_id` BIGINT NOT NULL COMMENT '博客ID',
    `user_id` BIGINT NOT NULL COMMENT '评论者ID',
    `parent_id` BIGINT DEFAULT NULL COMMENT '父评论ID(用于回复)',
    `content` TEXT NOT NULL COMMENT '评论内容',
    `like_count` INT DEFAULT 0 COMMENT '点赞数',
    `status` TINYINT DEFAULT 1 COMMENT '状态: 0-隐藏, 1-正常',
    `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    FOREIGN KEY (`blog_id`) REFERENCES `blog`(`id`) ON DELETE CASCADE,
    FOREIGN KEY (`user_id`) REFERENCES `user`(`id`),
    FOREIGN KEY (`parent_id`) REFERENCES `comment`(`id`) ON DELETE CASCADE,
    INDEX `idx_blog_id` (`blog_id`),
    INDEX `idx_user_id` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='评论表';

-- 点赞记录表
CREATE TABLE `like_record` (
    `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
    `user_id` BIGINT NOT NULL COMMENT '用户ID',
    `target_id` BIGINT NOT NULL COMMENT '目标ID',
    `type` TINYINT NOT NULL COMMENT '类型: 1-博客, 2-评论',
    `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (`user_id`) REFERENCES `user`(`id`),
    UNIQUE INDEX `idx_user_target` (`user_id`, `target_id`, `type`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='点赞记录表';
```

---

## 🔌 API 接口设计

### 接口规范

**统一响应格式：**
```json
{
    "code": 200,
    "message": "success",
    "data": {}
}
```

### 接口列表

#### 认证模块 `/api/auth`

| 方法 | 路径                 | 说明             |
| ---- | -------------------- | ---------------- |
| POST | `/api/auth/register` | 用户注册         |
| POST | `/api/auth/login`    | 用户登录         |
| POST | `/api/auth/logout`   | 用户登出         |
| GET  | `/api/auth/info`     | 获取当前用户信息 |
| POST | `/api/auth/refresh`  | 刷新Token        |

#### 用户模块 `/api/user`

| 方法 | 路径                 | 说明         |
| ---- | -------------------- | ------------ |
| GET  | `/api/user/{id}`     | 获取用户信息 |
| PUT  | `/api/user/profile`  | 更新个人信息 |
| PUT  | `/api/user/password` | 修改密码     |
| POST | `/api/user/avatar`   | 上传头像     |

#### 博客模块 `/api/blog`

| 方法   | 路径                      | 说明               |
| ------ | ------------------------- | ------------------ |
| GET    | `/api/blog/list`          | 获取博客列表(分页) |
| GET    | `/api/blog/{id}`          | 获取博客详情       |
| POST   | `/api/blog`               | 发表博客           |
| PUT    | `/api/blog/{id}`          | 更新博客           |
| DELETE | `/api/blog/{id}`          | 删除博客           |
| GET    | `/api/blog/search`        | 搜索博客           |
| GET    | `/api/blog/user/{userId}` | 获取用户的博客     |
| POST   | `/api/blog/{id}/like`     | 点赞博客           |

#### 分类模块 `/api/category`

| 方法 | 路径                       | 说明             |
| ---- | -------------------------- | ---------------- |
| GET  | `/api/category/list`       | 获取分类列表     |
| GET  | `/api/category/{id}/blogs` | 获取分类下的博客 |

#### 标签模块 `/api/tag`

| 方法 | 路径            | 说明         |
| ---- | --------------- | ------------ |
| GET  | `/api/tag/list` | 获取标签列表 |
| GET  | `/api/tag/hot`  | 获取热门标签 |

#### 评论模块 `/api/comment`

| 方法   | 路径                         | 说明         |
| ------ | ---------------------------- | ------------ |
| GET    | `/api/comment/blog/{blogId}` | 获取博客评论 |
| POST   | `/api/comment`               | 发表评论     |
| DELETE | `/api/comment/{id}`          | 删除评论     |
| POST   | `/api/comment/{id}/like`     | 点赞评论     |

---

## 📅 开发任务与排期

### 阶段一：环境搭建与基础架构（第1-2周）

| 任务         | 负责人 | 时间 | 产出物        |
| ------------ | ------ | ---- | ------------- |
| 环境配置     | 全员   | 2天  | 开发环境      |
| 数据库设计   | 后端   | 2天  | ER图、SQL脚本 |
| 项目骨架搭建 | 全员   | 3天  | 基础代码框架  |
| 接口文档设计 | 全员   | 2天  | API文档       |
| 需求分析文档 | PM     | 3天  | 需求文档      |

### 阶段二：后端核心开发（第3-5周）

| 任务         | 负责人 | 时间 | 产出物      |
| ------------ | ------ | ---- | ----------- |
| 用户认证模块 | 后端A  | 5天  | 登录注册API |
| 博客CRUD模块 | 后端B  | 5天  | 博客管理API |
| 搜索功能     | 后端A  | 3天  | 搜索API     |
| 评论模块     | 后端B  | 3天  | 评论API     |
| Redis缓存    | 后端A  | 2天  | 缓存配置    |
| 单元测试     | 后端   | 3天  | 测试用例    |

### 阶段三：前端核心开发（第3-6周）

| 任务         | 负责人 | 时间 | 产出物     |
| ------------ | ------ | ---- | ---------- |
| 登录注册页   | 前端A  | 3天  | 2个页面    |
| 首页与列表页 | 前端B  | 4天  | 2个页面    |
| 博客详情页   | 前端A  | 3天  | 1个页面    |
| 博客编辑页   | 前端B  | 4天  | 1个页面    |
| 用户中心页   | 前端A  | 4天  | 2个页面    |
| 搜索结果页   | 前端B  | 2天  | 1个页面    |
| 响应式适配   | 前端   | 3天  | 移动端适配 |

### 阶段四：联调与测试（第7周）

| 任务       | 负责人 | 时间 | 产出物   |
| ---------- | ------ | ---- | -------- |
| 前后端联调 | 全员   | 3天  | 联调完成 |
| Bug修复    | 全员   | 2天  | 修复报告 |
| 性能优化   | 全员   | 2天  | 优化报告 |

### 阶段五：文档与收尾（第8周）

| 任务         | 负责人 | 时间 | 产出物   |
| ------------ | ------ | ---- | -------- |
| 项目文档完善 | PM     | 3天  | 完整文档 |
| 演示视频录制 | 全员   | 1天  | 演示视频 |
| PPT制作      | PM     | 2天  | 汇报PPT  |
| 代码整理     | 全员   | 1天  | 最终代码 |

---

## 📝 需求分析概要

### 功能性需求

```
┌─────────────────────────────────────────────────────────────┐
│                      博客平台系统                            │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │
│  │   用户管理   │  │   博客管理   │  │   互动功能   │         │
│  ├─────────────┤  ├─────────────┤  ├─────────────┤         │
│  │ • 用户注册   │  │ • 发表博客   │  │ • 评论功能   │         │
│  │ • 用户登录   │  │ • 编辑博客   │  │ • 点赞功能   │         │
│  │ • 信息管理   │  │ • 删除博客   │  │ • 搜索功能   │         │
│  │ • 密码修改   │  │ • 博客分类   │  │ • 浏览统计   │         │
│  │ • 头像上传   │  │ • 博客标签   │  │             │         │
│  └─────────────┘  └─────────────┘  └─────────────┘         │
└─────────────────────────────────────────────────────────────┘
```

### 用例图

```
                    ┌──────────────────────┐
                    │      博客平台系统      │
                    └──────────────────────┘
                              │
        ┌─────────────────────┼─────────────────────┐
        │                     │                     │
        ▼                     ▼                     ▼
   ┌─────────┐          ┌─────────┐          ┌─────────┐
   │ 游客    │          │ 用户    │          │ 管理员  │
   └─────────┘          └─────────┘          └─────────┘
        │                     │                     │
        │ • 浏览博客          │ • 发表博客          │ • 用户管理
        │ • 搜索博客          │ • 编辑博客          │ • 内容审核
        │ • 注册登录          │ • 评论点赞          │ • 分类管理
        │                     │ • 个人中心          │ • 系统设置
```

---

## 🏃 启动命令

### 开发环境启动

```powershell
# 1. 启动 MySQL
net start mysql

# 2. 启动 Redis
redis-server

# 3. 启动后端 (新终端)
cd backend
mvn spring-boot:run

# 4. 启动前端 (新终端)
cd frontend
pnpm dev
```

### 或使用根目录命令

```powershell
# 启动前端
pnpm dev:frontend

# 启动后端
pnpm dev:backend
```

### 访问地址

- 前端页面：http://localhost:5173
- 后端接口：http://localhost:8080
- API文档：http://localhost:8080/doc.html

---

## 📋 文档清单

根据课程要求，需要提交以下文档：

1. **需求分析文档** - 用例图、功能描述
2. **可行性分析文档** - 技术/经济可行性
3. **概要设计文档** - 架构图、分析类图
4. **详细设计文档** - 类图、时序图、活动图
5. **项目分工说明** - 团队分工表
6. **开发过程文档** - 问题清单、讨论记录
7. **项目演示视频** - 功能演示
8. **汇报PPT** - 答辩用

---

## ✅ 检查清单

- [ ] 环境配置完成 (JDK, Node.js, MySQL, Redis)
- [ ] 数据库创建并初始化
- [ ] 后端项目可正常启动
- [ ] 前端项目可正常启动
- [ ] 前后端可正常通信
- [ ] 8个以上页面完成
- [ ] 响应式设计完成
- [ ] API文档完整
- [ ] 项目文档完成
- [ ] 演示视频录制完成
